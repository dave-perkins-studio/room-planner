# room-planner

A signpost, not a project. This repository exists only so the old address

    https://dave-perkins-studio.github.io/room-planner/

keeps working after the tool was renamed to Chalk. GitHub redirects a renamed
repository but not its Pages URL, so anything sent to family before the rename
would otherwise have died.

The tool itself lives at [dave-perkins-studio/chalk](https://github.com/dave-perkins-studio/chalk),
served at <https://dave-perkins-studio.github.io/chalk/>.

`index.html` and `404.html` are the same page, so a deep link to any path here
forwards too. Nothing needs maintaining. Delete it if the old link stops
mattering.
